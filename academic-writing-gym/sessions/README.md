# Session records

One directory per session: `YYYY-MM-DD-<topic>/` containing `version-1.md`,
`diagnosis.md`, `version-2.md`, and `comparison.md`.

Kept out of the error cards deliberately: cards hold the pattern and a short
quoted instance, sessions hold the full text. Reread the cards; archive these.
