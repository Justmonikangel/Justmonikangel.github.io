import { RoutePlaceholder } from '@/components/common/RoutePlaceholder';

export default function ReportDetail() {
  return <RoutePlaceholder title="Report Detail" description="P3 再补齐指标解读、趋势图和 Rotterdam 可视化。" />;
}
