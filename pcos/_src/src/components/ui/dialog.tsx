// TODO P1: Replace with shadcn-generated <Dialog>. Until then, throw on
//         render so any premature consumer fails loudly rather than
//         silently rendering nothing.
export function Dialog() {
  throw new Error('UI component <Dialog> is not implemented yet (P1).');
}
